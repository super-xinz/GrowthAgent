import Link from "next/link";import {getOpportunities} from "@/lib/api";
export default async function Opportunities({params}:{params:Promise<{id:string}>}){const {id}=await params;const rows=await getOpportunities(id);return <><div className="eyebrow">Conversation radar</div><h1>Opportunities</h1><p>Policy, generated replies, and publish status are visible for audit. There is still no per-comment approval queue.</p><div className="actions"><Link className="button secondary" href={`/products/${id}`}>Product</Link><Link className="button secondary" href={`/products/${id}/safety`}>Safety</Link></div><div className="card"><table className="table"><thead><tr><th>Conversation</th><th>Community</th><th>Intent</th><th>Score</th><th>Policy</th><th>Reply</th></tr></thead><tbody>{rows.map((x:any)=><tr key={x.id}><td><strong>{x.title||"Comment"}</strong><br/><span className="label">{x.body.slice(0,150)}</span></td><td>r/{x.subreddit}</td><td>{x.intent_label.replaceAll("_"," ")}</td><td className="score">{Math.round(x.opportunity_score*100)}<br/><span className="label">risk {Math.round(x.risk_score*100)}</span></td><td><span className="status">{x.policy_decision||"pending"}</span><br/><span className="label">{x.publish_status||"not published"}</span></td><td className="reply">{x.generated_reply?x.generated_reply.slice(0,260):"Run fixture import to generate a shadow reply."}</td></tr>)}{!rows.length&&<tr><td colSpan={6}>No fixtures imported yet. Use <code>make seed</code> after building a Product Brain.</td></tr>}</tbody></table></div></>}
