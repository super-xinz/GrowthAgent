import Link from "next/link";import {getConversations} from "@/lib/api";
export default async function Conversations({params}:{params:Promise<{id:string}>}){const {id}=await params;const rows=await getConversations(id);return <><div className="eyebrow">Conversation loop</div><h1>Conversations</h1><p>Each shadow or mock-published reply opens a conversation record with follow-up state, conversion state, and stop controls in the API.</p><div className="actions"><Link className="button secondary" href={`/products/${id}`}>Product</Link><Link className="button secondary" href={`/products/${id}/opportunities`}>Opportunities</Link></div><div className="card"><table className="table"><thead><tr><th>State</th><th>Conversion</th><th>Followups</th><th>Last activity</th><th>Next check</th><th>Closed reason</th></tr></thead><tbody>{rows.map((x:any)=><tr key={x.id}><td><span className="status">{x.state}</span></td><td>{x.conversion_state}</td><td>{x.followup_count}</td><td>{new Date(x.last_activity_at).toLocaleString()}</td><td>{x.next_check_at?new Date(x.next_check_at).toLocaleString():"-"}</td><td>{x.closed_reason||"-"}</td></tr>)}{!rows.length&&<tr><td colSpan={6}>No conversations yet. Generate and publish or shadow-record an opportunity first.</td></tr>}</tbody></table></div></>}
